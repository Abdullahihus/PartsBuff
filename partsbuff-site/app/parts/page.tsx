import Link from "next/link";
import { formatMoney, parts } from "@/data/inventory";
export default function PartsPage(){return <section className="section shell"><div className="page-hero"><div className="eyebrow">USED PARTS</div><h1>Find quality used auto parts</h1><p>This starter inventory is ready to be connected to PostgreSQL/Supabase for real-time search and stock.</p></div><div className="table-card"><div className="parts-table parts-head"><span>Part</span><span>Vehicle</span><span>Condition</span><span>Price</span></div>{parts.map(p=><div className="parts-table" key={p.id}><span><strong>{p.name}</strong><small>{p.category} · {p.stock}</small></span><span>{p.vehicle}</span><span>{p.condition}</span><span><strong>{formatMoney(p.price)}</strong></span></div>)}</div><div className="center-cta"><h2>Don’t see your part?</h2><p>Send us your vehicle information and what you need.</p><Link href="/contact" className="button">Request a part</Link></div></section>}
