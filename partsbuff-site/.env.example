# Local Docker database password. Set before starting Compose.
POSTGRES_PASSWORD=replace-with-a-local-password
# Optional: use DATABASE_URL when running Node outside Docker.
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/partsbuff
NEXT_PUBLIC_SITE_URL=http://localhost:3000
